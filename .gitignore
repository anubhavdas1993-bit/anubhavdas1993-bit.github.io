# Astro build output
dist/
.astro/

# Dependencies
node_modules/

# Environment
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log

# Local Claude tooling — rules, commands, hooks, settings (personal, kept local)
.claude/
CLAUDE.md
.session/

# ── Personal / source documents — NEVER commit ──
# (the originals now live outside the repo in ../Portfolio_Private_Source/)
Profile_Files/
*.docx
*.pdf
# ...but a résumé you deliberately publish lives in public/ — allow that one:
!public/**/*.pdf

# ── Throwaway design exploration & screenshots (kept locally as build reference) ──
prototypes/

# ── Internal planning / process notes (kept locally, not shipped publicly) ──
docs/superpowers/
docs/NEW-SESSION-PROMPT.md
docs/GUARDRAILS.md

# ── OS / editor extras ──
Desktop.ini
ehthumbs.db
.idea/
.vscode/
*.swp
